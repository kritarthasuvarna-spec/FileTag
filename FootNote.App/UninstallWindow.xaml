<Window x:Class="FootNote.App.UninstallWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Uninstall FootNote" Width="480" Height="320" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen" Background="#1E1E2B">

    <Window.Resources>
        <SolidColorBrush x:Key="Text" Color="#F2F2F7"/>
        <SolidColorBrush x:Key="Dim" Color="#9B9BA8"/>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="16,7"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="Background" Value="#3D3D4D"/>
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <!-- 1. Confirm -->
        <Grid x:Name="ConfirmPanel">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Uninstall FootNote?" FontSize="20" FontWeight="SemiBold"/>
            <TextBlock Grid.Row="1" x:Name="ConfirmText" TextWrapping="Wrap" FontSize="13"
                       Foreground="{StaticResource Dim}" Margin="0,14,0,0" LineHeight="20"/>
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Click="Cancel_Click"/>
                <Button x:Name="UninstallBtn" Content="Uninstall" Background="#C24242" Click="Uninstall_Click"/>
            </StackPanel>
        </Grid>

        <!-- 2. Progress -->
        <Grid x:Name="ProgressPanel" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Uninstalling…" FontSize="20" FontWeight="SemiBold"/>
            <ProgressBar Grid.Row="1" x:Name="Progress" Height="10" Margin="0,24,0,10"
                         Background="#3D3D4D" BorderThickness="0" Foreground="#C24242"/>
            <TextBlock Grid.Row="2" x:Name="ProgressText" FontSize="12" Foreground="{StaticResource Dim}"/>
        </Grid>

        <!-- 3. Finish -->
        <Grid x:Name="FinishPanel" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="FootNote has been removed" FontSize="20" FontWeight="SemiBold"/>
            <StackPanel Grid.Row="1" Margin="0,14,0,0">
                <TextBlock x:Name="SummaryText" TextWrapping="Wrap" FontSize="13"
                           Foreground="{StaticResource Dim}" LineHeight="20"/>
                <TextBlock Margin="0,10,0,0" FontSize="12">
                    <Hyperlink x:Name="LogLink" Foreground="#6BA1F8" Click="LogLink_Click">Open uninstall log</Hyperlink>
                </TextBlock>
            </StackPanel>
            <Button Grid.Row="2" Content="Close" HorizontalAlignment="Right" Click="Finish_Click"/>
        </Grid>
    </Grid>
</Window>
